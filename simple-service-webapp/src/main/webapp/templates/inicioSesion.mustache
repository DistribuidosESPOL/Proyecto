<!DOCTYPE html>
<html>
  <head>
    <title>Iniciar sesión</title>
    <style>
    .row {
      line-height: 2;
    }
    .row .label {
      float: left;
      width: 40%;
      text-align: right;
      margin-right: 20px;
    }
    .toolbar {
      text-align: center;
      margin-top: 30px;
    }
    </style>
  </head>
  <body>
    <h2>Inicio de sesión</h2>
    <form action="/simple-service-webapp/micro/sesion/validar" method="POST">
      <input type="hidden" name="id" value="{{id}}" />
      <div class="row">
        <div class="label">Nombre de usuario</div>
        <div class="control">
          <input type="text" name="alias" value="{{alias}}" {{#alias}}readonly{{/alias}} required/>
        </div>
      </div>
      <div class="row">
        <div class="label">Contraseña</div>
        <div class="control">
          <input type="password" name="contrasena" value="{{contrasena}}" required/>
        </div>
      </div>
      <div class="toolbar">
        <input type="submit" value="Ingresar" /></br>
        <a href="/simple-service-webapp/micro/sesion/registroForm">Registrarse</a>
      </div>
    </form>
  </body>
</html>
