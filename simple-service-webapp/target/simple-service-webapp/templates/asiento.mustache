<!DOCTYPE html>
<html>
  <head>
    <title>Asiento</title>
    <style>
      .row {
        line-height: 2;
      }
      .header {
        font-weight: bold;
      }
      .col {
        float: left;
        width: 16%;
      }
      .toolbar {
        margin-bottom: 20px;
      }
      .info {
        font-style: italic;
      }
      input, select{
                margin-bottom: 15px;
        }
    </style>
  </head>
  <body>
    <h2>Seleccionar asientos</h2>
    <form action="/simple-service-webapp/micro/compra/pago/{{idEvento}}" method="POST">
        <label>Categoría de asiento</label>
        <select name="categoria" required>
            {{#listaAsientos}}
                {{categoria}}
            <option value="{{categoria}}">{{categoria}}</option>
            {{/listaAsientos}}
        </select></br>
        <label>Número de asientos</label>
        <input type="number" min="1" id="num_asientos" name="num_asientos" required></br>
        <label>Tipo de pago</label>
        <input type="submit" value="Siguiente"/>
        <input type="button" onclick="location.href='/simple-service-webapp/micro/eventos'" value="Cancelar">
    </form>
  </body>
</html>