<!DOCTYPE html>
<html>
  <head>
    <title>Registrarse</title>
    <style>
    .row {
      line-height: 2;
    }
    .row .label {
      float: left;
      width: 40%;
      text-align: right;
      margin-right: 20px;
    }
    .toolbar {
      text-align: center;
      margin-top: 30px;
    }
    </style>
  </head>
  <body>
    <h2>Registrar usuario</h2>
    <form action="{{^id}}/simple-service-webapp/micro/sesion/registrar{{/id}}" method="POST">
      <input type="hidden" name="id" value="{{id}}" />
      <div class="row">
        <div class="label">Nombre de usuario</div>
        <div class="control">
          <input type="text" name="alias" value="{{alias}}" minlength="3" maxlength="10" {{#alias}}readonly{{/alias}} />
        </div>
      </div>
      <div class="row">
        <div class="label">Contraseña</div>
        <div class="control">
          <input type="password" name="contrasena" value="{{contrasena}}" minlength="8" maxlength="16"/>
        </div>
      </div>
      <div class="toolbar">
        <input type="submit" value="Registrarse" /></br>
        <input type="button" onclick="location.href='/simple-service-webapp/micro/sesion'" value="Ya tengo una cuenta">
      </div>
    </form>
  </body>
</html>